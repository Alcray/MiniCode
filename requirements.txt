# Core dependencies
httpx>=0.25.0          # HTTP client for LLM API calls
rich>=13.0.0           # Terminal UI (lightweight)
pyyaml>=6.0            # Config file parsing
pathspec>=0.11.0       # Gitignore pattern matching
